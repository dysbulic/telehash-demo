var JSObject = Packages.netscape.javascript.JSObject
var window = JSObject.getWindow( hostApplet )

try {
    window.call( 'triggerAppletLoaded', [] )
} catch( ex ) {
    console.log( "Error in triggerAppletLoaded(): " + ex )
}
